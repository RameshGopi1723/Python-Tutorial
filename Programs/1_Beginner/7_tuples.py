def tupleIndex(tuple, indexValue):
    value = tuple[indexValue]
    return value

fruits = ("apple", "banana", "cherry", "Mango", "grapes", "strawberry")
print("fruit in the specified index: ", tupleIndex(fruits, 5))
